# 备案任务：标题/引擎菜单像素 UI（请 Codex 实现）

> 2026-09-08 Codex 更新：标题静态 Logo、按钮切片、坐标、高亮、解锁条件与点击热区已接入；
> 详见 `reports/title-ui-20260908.md`。其他菜单仍待完成。下文为此前任务背景，
> 剧情分支继续保持消息窗内纯文字无框，不属于部件框替换范围。

> 发起：DeepSeek 侧（2026-09-08）。原因：本任务需要目视原版画面做像素级对照，
> 当前模型无图像输入，已尽力完成全部可静态取证的前置工作，剩余部分移交 Codex。

## 目标
把引擎菜单（标题 Util37、读档 Util38、槽位 45/46、消息窗、选择框）从现在的
「自绘文本方块」改成**原版观感的像素 UI**：原版部件图合成 + 正确坐标 + 高亮 + 鼠标热区。

## 前置工作（已完成，直接使用）
1. **开机流程**（STARTUP.MES，259B）确认原版把 UI 部件图载入固定 surface：
   - heap=5 → `selparts.bmp`（选择高亮条 320x68）
   - heap=9 → `sl_pt.bmp`（槽位面板 656x464）
   - heap=10 → `setting.bmp`（设定面板 592x1096）
   - heap=11 → `menu.bmp`（菜单面板 840x372）
   - heap=6 → `mwaku.bmp`（消息窗框 640x208）
   - 运行时已按原版在 boot 加载（frontend 层这些 surface 已存在，但从未合成显示）。
2. **标题场景**（start.mes）：title_bg.gcc(dst1,640x480 底图) + title_pt.gcc(dst7,584x888，
   疑为标题按钮贴图条/部件)，之后进入引擎标题菜单 Util37。
3. **全部部件图已解码为 PNG**：`work/cgout/{title_bg,title_pt,selparts,mwaku,menu,sl_pt,setting}.png`
   （DeepSeek 与 Codex 两个工作区都有；解码工具 work/cgout/cgdec.c，用法 cgdec gcc.ARC 条目 输出.png）。
   当前自绘标题参考帧：`work/title-current.png`（菜单框位于 100,100..539,187 的现状）。

## 待 Codex 完成（需要目视/实机对照）
1. 对照原版（实机 PC 版或用户提供的原版截图）确定各菜单项的**屏幕坐标与样式**；
2. 实现合成：部件图 blit + 项文本（原版字体观感）+ 选中高亮（selparts）+ 方向键/鼠标；
3. 消息窗与选择框改为用 mwaku/menu 等部件的原版样式；
4. host 目视验证 + Switch 实机验证后回填本备案。

## 已探明的引擎线索（供参考，未深入）
- AI.exe 引擎菜单分发在 `0x43eb2c`（Util37 标题）：实际是 case 分发板
  （0x43eb78/0x43eb88/0x43eb94/... 分别对应标题/读档/……，各调 0x401xxx 菜单框架方法），
  像素绘制坐标埋在 C++ 菜单框架里，直接提取成本高，建议以目视对照为主。
- 标题项文本不在 AI.exe 里（搜过 SJIS/UTF-16，只有「ロード」在 0x4530 UTF-16），
  强烈暗示菜单项是 **title_pt.gcc 里的按钮位图**，引擎只负责定位/高亮——请先目视 title_pt.png 确认。
- 引擎菜单只在 var4[152]==1（看过结局）后才显示 アルバム/シーン/サウンド/エンディング，
  否则仅 はじめから/ロード（vm.c util case37 已按此门控）。

## 顺带工具（本轮已加，已提交）
- KAWA_FRAMES 现在非 smoke 也生效（截图用帧上限）；配合 `--screenshot out.bmp` 可抓任意画面帧。

## 追加核验（2026-09-08，消息窗实现已按用户逐步校准，暂定为当前观感）
- 引擎消息窗区域 = 底部 640×104（AI.exe 0x4391d0 用 0x280×0x68；screen y≈376..479）。
- mwaku.gcc(640×208)=上、下两个 104 高框叠放；**只取下半框 rows 104..207** 上屏（之前整块贴=“两个框”错误）。
- 实现：框/亮部(g>118 或边缘)不透明贴；内芯向**中性暗灰幕**混合（当前 a=194 ≈76% 覆盖、灰 g=40，
  用户逐轮校准至“差不多”：不太黑也不太淡，背后人物/背景仍保留色彩、非全灰）。
- 待 Codex 实机/对照 PC 核验：① 窗内幕的最终明暗/灰调是否一致；② 框线位置/样式（mwaku 下半框的
  row/col 边界与我们的 g>118 分类是否相符）；③ 文字起点/行距（当前 shadow@(28,392)/600 宽）。

## 追加核验（2026-09-08，主菜单/标题）
- DeepSeek 侧已确认：exe 内**不存在**标题项文字串（SJIS/UTF-16 逐字节扫全文件，仅系统菜单串
  セーブ/ロード/環境設定/ゲームの終了/最小化等；无 はじめから/アルバム/シーン/サウンド/エンディング）
  → 强烈暗示**标题项是位图**（title_pt.gcc 584×888, dst7，疑为按钮行切片），引擎只定位/高亮。
- 引擎菜单分发 0x43eb2c（case 表 0x3ee94: [12]=37 标题, [13]=38 读档, [16]=41 声音…）；
  菜单框架在 0x401xxx 系列 C++ 方法，绘制坐标需继续追；无视觉下硬猜坐标风险高（quake 前车之鉴）。
- 现实现仍为文字列表(自绘框)。**请 Codex 后期按实机/PC 对照完成**：
  ① 确认标题项=title_pt 位图切片并给出行高/屏幕坐标/点击热区；② 高亮(selparts)用法；
  ③ 文字项 vs 位图项取舍；④ 引擎字体与原版位图字体差异处理。
