# AX 播放器：2026-09-08 验证记录

## 关键纠正

旧交接的“绘制函数深埋回调，无法实现”结论有误。0x4932d8 是抽象基类虚表，
0x470041 是 pure-call 处理路径，不是像素绘制的惰性分发。
构造函数 0x409c30 调用基类构造后，把虚表改为 **0x493358**。
实际绘制函数就在派生类中：

| 槽位 | 实际地址 | 行为 |
|---|---|---|
| +0x30 / kind 0 | 0x409da0 | 透明合成 surface 8 → 1，复制更新区域 1 → 0 |
| +0x34 / kind 1 | 0x409e50 | 同上 |
| +0x38 / kind 2 | 0x409f00 | 同上 |
| +0x3c / 其它 kind | 0x409fb0 | 空函数 |

显示对象 +0x80/+0x84/+0xa0 分别对应 surface 0/1/8。
surface 虚表 0x4941e0，+0x3c → 0x41b160：跳过 RGB(0,255,0)，
其它像素按 alpha 合成；各色通道为 floor(src*a/255)+floor(dst*(255-a)/255)。
这与常见的一次除法或 SDL 近似 alpha 混合存在 1 级颜色差异。
surface +0x1c → 0x41ab10 为不透明复制。AX 渲染无须读取 MSK；MSK 属于其它画面效果。

var32[14] 也被旧交接误称为“动画启用开关”。在 AX 资源装载函数 0x410570 中，
0x4106b6 明确写 object+0x510b8=0（成功），失败分支写 1。
脚本的 `jz var32[14] == 0` 在成功时执行后面的 Anim.start。不能硬编码为 1。
这也意味着旧路线探索对该变量的解释需重新核对，不能为路线测试修改它。

## 二进制与状态机

- 头部 0x500 字节：320 个 little-endian u32 程序入口；槽索引=bank*32+cell，10×32。
- 描述符从 0x500 起，28 字节 `{kind,sx,sy,width,height,dx,dy}`。它们不是入口表中的“帧”。
- 文件大小上限 0x5a00，来自原播放器的缓冲大小；现实现按实际文件大小检查每次读取。
- 构造函数 0x40981d–0x409827 注册 0x14（20ms）周期任务。0x4096c0 按槽序推进。
- 每 tick 只执行一条指令；delay 非零先减一并返回，再处理 boundary_delay。
- 0=运行；1=运行至下个边界停止；3=运行至边界暂停；4=暂停；255=停止。

| 指令 | 语义（0x409900–0x409b11） |
|---|---|
| 0 | 无操作 |
| 1 + u32 | 写边界延时；state 1→255，3→4 |
| 2 + u32 | 写普通延时 |
| 3 | 当前程序 ip=0 |
| 4 / 255 | 停止 |
| 5 / 7 + u32 | 设置循环 1 / 2 的返回点与次数 |
| 6 / 8 | 循环尾；总次数 0 表示无限循环 |
| 其它 + u32 | 取描述符并分发绘制 |

Anim 控制由 0x410570 和虚表确认：1 续播，2 边界停止，3 续播并等待结束，
4 停止指定槽，7 对活动槽请求边界停止，10/11 停止全部。4 的 PC 实现将参数 1
同时用于 bank 和 cell，本实现保留。其它编号（包括 26）是原 dispatcher 的空洞。
控制命令不会重置播放位置；新 AX 装载才重置。同步等待保留 SDL 事件处理，避免锁死窗口。

## 实现与存档

- runtime/ax.c：不依赖 SDL 的有界解释器，draw callback 分离绘制。
- runtime/ax_render.c：原版遮罩像素算法；同时用于普通 masked copy。
- runtime/frontend.c：独立于对白/选项等待推进动画，最多补算 200ms，避免系统恢复后补跑过久。
- runtime/vm.c：AX 装载、控制、同步等待。
- KWS v2：旧 VM 与图层段之后追加 37,196 字节 ax_player 快照；包含 AX 数据、文件名、320 槽状态、
  播放位置、两套循环计数与不足一 tick 的余量。字段均为固定宽度，不保存指针。
- 新程序读取 v1 / v2，读取完成并验证后才替换当前场景。v1 没有动画信息，初始化为停止。

## 本轮证据及边界

tests/ax_test.c 的合成样本验证确定的时间边界、循环/续播和像素结果。
同一测试还可扫描原版 sequence.ARC：117 文件×320 槽×2,000 tick，
累计 28,173 次绘制，无读取/状态错误。无限循环的全语料测试有时间上限。

实际剧情不是只跑计数器：序章 004.ax 已在暂停对白时依次绘制 srcY=0,24,48,24,0，
从 surface 8 抽取 84×24 的眼睛帧。跨进程从槽 3 恢复也验证了同样的继续播放。
reports/ax-open.png 与 ax-blink.png 是同一存档的实际运行截图，515 个变化像素
全部位于眼睛区域 (290,80)–(364,94)。旧 KWS v1 的读取及再保存也通过验证。
主线 1,200 条对白经过 s1/s4/s5/s6/s7/s11/over2，没有 STOP。

这建立了静态原程序证据、确定性单测和实际资源运行三层验证；还没有原版 PC 同时刻的
逐像素/逐帧对照，也没有本轮 NRO 的 Switch 实机画面确认。其它 UI、转场和震屏不属于
已经完成的 AX 功能，不能据此声称整个游戏 100% 复刻。
