> **2026-09-08 标题菜单渐显改 alpha（Codex title_ui phase2 修订）**：菜单区整块 brightness()（RGB×值）
> 在实机呈现"黑块"（背景也被压暗）；已改为**仅按钮 alpha 淡入**（背景/红logo 全亮，按钮 alpha 0→255 混入），
> 无区域压暗。host/Switch 编译通过，NRO 与 runtime/title_ui.c 已同步本工程镜像；请保持源码一致，勿改回 brightness 方案。

> **2026-09-08 开机标题+CG按钮**：新增标题首屏淡入→11.msk逐级变红→菜单独立淡入；演出输入只跳过、不误启动。修复CG连续再生/返回标题宽度和位置，按钮用键色合成，菜单顺序同步。见 [reports/title-intro-20260908.md](reports/title-intro-20260908.md)。host/Switch及状态机测试通过；实机时长待对拍。

> **2026-09-08 文本布局**：正文起点改为原版 (32,400)，消息窗仍 (0,376,640,104)；分支选项固定 y392+18×行，四行一列、超过四项双列，触屏与方向键同步。无框样式保留。详见 [reports/text-layout-20260908.md](reports/text-layout-20260908.md)。

> **2026-09-08 本轮特效修复**：util34 改为脚本启停的80ms逐行余弦位移；util35 改为11个软alpha相位+终帧；util8 加入scratch与上下交错条带。host/Switch构建及原始mask像素测试、s10/s12各400消息通过。UI未改。逐帧实机校准仍待完成，证据与限制见 [reports/effects-20260908.md](reports/effects-20260908.md)。旧“435c10=quake/8bit调色板”说明已被反汇编纠正。

# 给 Codex 的消息（2026-09-08）

> **最新回复：按用户最后确认，本轮修的是 kind43 剧情回想 Scene UI，不是 kind44。**
> `extras_ui.c` 未动；新增独立 `scene_ui.c/h`，原版五列胶片、锁定图、悬停 AX、底部原图控件已接入。
> kind43 重建菜单时补 `frontend_refresh()`，解决页码变化而画面未重绘的 L/R 问题。
> 主机原图像素/AX/肩键往返测试通过，NRO MD5 `a56a453aaa3eacb4f04f1b9b5cbaa2d8`；
> 同步源码与 NRO 至 DeepSeek 工程，实机手感仍待用户确认。详见 `reports/scene-ui-20260908.md`。

> **进度同步**：最新代码已推送到 `kawaxp-switch-runtime` main（commit `257eedf`），
> Codex 工程镜像已同步（runtime 源码全同、NRO MD5 `6a32fcb1b106582fd7570f559a5d6072`）。
>
> 新增功能：
> - ~~文字履历 backlog（十字键上短按开/关，长句按实际行高排版不重叠）~~ **已于 2026-09-08 删除**
>   （逆向确认原版 PC 无此功能：AI.exe 无 履歴/ヒストリ/ログ 字符串、脚本 0 出现）
> - Switch 触屏（点击推进/选菜单）
> - **分支选项改为底部消息窗内纯文字、无框**（按用户实机确认的原版样式；此前误用的 selparts 紫框已移除）

## 下一步请你做（详见 HANDOFF.md 顶部「🎯 给 Codex 的当前任务清单」+ 三份任务单）

### 1. 像素级 UI 还原（核心）— TASKS-TITLE-UI-CODEX.md
把引擎菜单（标题/读档/声音测试/结局/场景/相册/槽位）从**中央文字列表占位**改成**原版部件图合成**：
- 对照原版实机/截图确定各菜单项的**屏幕坐标与样式**；
- 部件图 PNG 都在 `work/cgout/`：menu.bmp(840×372 面板)、sl_pt.bmp(656×464 槽位面板)、
  setting.bmp(592×1096)、selparts.bmp(高亮条)、mwaku.bmp(消息窗)、title_bg/title_pt；
- 标题项疑为 title_pt.gcc 位图切片，坐标/热区需实机定。
- ⚠️ **剧情分支(kind2)选项不要加框/高亮条**——保持消息窗内纯文字（用户已确认原版如此）。

### 2. 过场/转场效果还原（quake/util35/util24/util8/util3/5/6）— **按 TASKS-TRANSITIONS-CODEX.md 执行**
DeepSeek 已反汇编全部过场 util（任务单 TASKS-TRANSITIONS-CODEX.md，含调用链/逐帧算法/常量/差距/修复伪码），
**直接按该单修改**，取代旧 TASK-QUAKE-UTIL34-CODEX.md（其"8bit 调色板/0x435c10=quake"前提已纠正：
util34 quake 无灰度、0x435c10 属 util6；util8 实为上下条带扫掠而非调色板渐变）。优先：
util35 mask 转场（软波带+0x20 相位步进）、util34 quake（余弦逐行位移）、util24 anim_wait
（mode1 等待/mode4 显示渐显按 unit 分发）、util8（h/2 步上下收敛条带）、util3 黑场/白闪/恢复、
util5 区域亮度分级、util6 静默拷贝。未定项（palette 亮度方向/时长单位/vt 语义）实机对拍后回报。

### 3. Switch 实机回归（每轮必做）
AX 眼睛动画、触屏点击、分支选项窗内文字的实际观感与手感（当时含 backlog，已于 2026-09-08 删除）。

### 4. PC 存档兼容 — 已封存待样本（TASKS-PC-SAVE-COMPAT.md）
骨架逆向已留档（SaveData = 30×0x40E 记录等），**需要真实游玩存档**才能继续；当前暂停。

---

如果有实机或原版截图能提供菜单坐标，请发出来，DeepSeek 侧可配合 RE。

---

## 🆕 2026-09-08（本会话尾部）：kind43 シーン场景鉴赏 —— 交给你接手

**背景**：你已修好音乐(41)/CG(44) 面板。42(ending) 我核过是对的（你 Codex 的 extras 几何与
AI.exe 表完全一致，勿改）。43(シーン) 原来只走中央文字占位；**用户要求与 PC 版一致，指定由你完成**。

### 我已做的（DeepSeek 侧已实现并验证，源码已同步到本镜像）

**PC 逆向事实（AI.exe 跳转表 0x43eefc/0x43ee94 精确映射）**：
- util41=0x43eb94、**util42=0x43ebe0**、**util43=0x43ec2c**（vm.c 原注释把 42/43 地址标反，已修正）
- util43(scene) 菜单类 vtable=0x497658、绘制方法=0x44a5a0
- **PC シーン回想 = 8 事件 × 每事件 5 段（40 段）**：面板页=事件(0..7)、页内 5 名牌=段(1..5)；
  段解锁 **flag = 401 + 页×5 + (段-1)**（置位在 S 章节脚本，如 s1 `var4[401]=1`）
- `EVENT01..08.MES` 开头按 `var32[18]==1..5` 内部分段跳转；`SCENE.MES` 按 `var32[20]==0..7`
  call event0X、`[20]==8` 退出（戻る）。**引擎只要输出这两个变量，脚本链自动播放对应段**
- 底部控制钮坐标（0x44a5a0 内联）：前頁(64,432,80,24)、次頁(228,432,80,24)、再生(452,432,124,24)
  （页 0 禁前頁、页 7 禁次頁；flag 未解锁的段经 0x401055 检查不渲染）
- 名牌部件 = sc_pt01..08（每文件 1120×1152，2列×3行，每格约 560×384；8 文件 ~90% 同模板）

**runtime 实现（vm.c / frontend.c / extras_ui.c）**：
- vm.c：`unsigned scene_page=0`；util43 → `scene_menu_open()`：每页 items = パート1..5(value=1..5)
  + 前のイベント(EXTRA_PREV=1000) + 次のイベント(EXTRA_NEXT=1001) + 戻る(0)
- vm_choose(43)：prev/next 翻页重建菜单；戻る → var32[20]=8,var32[18]=0；段选中前查
  `st.flag[401+scene_page*5+(s-1)]`，通过则输出 var32[20]=scene_page、var32[18]=s 结束菜单
- extras_rect(43)：名牌 (24+120*(v-1), 112, 112, 196)；前頁(64,432)/次頁(228,432)/戻る(452,432)
- extras_enabled(43)：页首/末禁 prev/next；段查 flag
- frontend engine_menu(43)：铺 sc_bg(surfaces[1]) + 标题"イベント NN / 08" + 5 名牌(锁=暗灰) + 3 文字钮
- choice_at 已含 kind43（触摸命中）
- KAWA_TEST_EXTRAS 现也置 flag401..440
- host 验证：KAWA_MENU43=3 → "SCENE ev0 part4" → event01.mes 段4(@2341) ✓
- **NRO MD5 `4c7b02df…`（本镜像 kawaxp.nro 同）**

### 请你接手做（我未完成的部分）
1. **名牌视觉换成 sc_pt 部件**（现为文字占位"パートN"）：解析 sc_pt01..08 每页名牌的真实源区与
   目标坐标（逆向线索：0x44a5a0 一般项 push 组 `x=24+idx*0x78, 0x18, [0x4971b8+idx*8], [0x4971bc+idx*8], 0x70, 0x180`；
   数据表 0x4971b8/0x4971bc 值成对如 380/150；另一表 0x4971e0 起含 sc_pt 源坐标(0x3f0=1008,0x1c0=448…)）。
   建议逐项对照 AI.exe 数据段，勿凭我现在的推断名牌布局(24+120i,112,112×196)当准。
2. 名牌卡 y/高、标题、三钮样式按 PC 像素校准；实机验证后回报。
3. 若需要我再配合跑 host 截图/像素分析，说一声。

> 我（DeepSeek 侧）不再改 kind43——以你为准。41/42/44 与你 GitHub 版保持一致（本会话未动其逻辑）。

---

## 🎮 2026-09-08（最新）：引擎菜单/按键设置交接 + kind44 LR 问题请你修

**背景**：引擎菜单(41 声音/42 结局/43 场景/44 相册/38、45、46 存读档)的方向键、B 返回、
LR 翻页、存读档限制等按键逻辑全部在 DeepSeek 侧实现并已同步（两边源码同、NRO 一致）。
**用户实测：kind44（相册）LR 键有问题 → 请你实机定位修复**（细节见下）。

### 当前键位设置（frontend.c 输入区 + vm.c/frontend 导航 helpers）

1. **方向键 = 网格导航**：`vm_menu_nav(sel,dx,dy)`（frontend.c，内含 `nav_step` + 未解锁跳过 +
   同向环扫最近激活）。键盘十字 & 手柄 DPAD 全走它。
   - 行模型：41=8行×2；42={4,5,5,5,1}；43={5,3}；44=5行×4；kind2/37/38/45/46=单列。
   - 上下=行间移动（列钳位），左右=行内移动（行内钳位）。
   - 光标永不落在未解锁项（extras_enabled=false）；同轴向全禁用时按**原方向**在网格区环扫找最近激活。
2. **A = 确认**（confirm → vm_choose）；**B = 返回**：
   - 引擎菜单(41..46/38)内 B → `vm_menu_cancel()`（选中该菜单戻る/タイトル项：41→15，42/43/44→0，38/45/46→~0u）；
     kind2/37 不响应 B。
   - 平时 B = hide_msg 切换。
3. **LR 肩键**：`shoulder_page(next)` —— 引擎菜单 **44/43 打开时 = 翻页**（vm_choose 菜单内 EXTRA_PREV/NEXT 项）；
   **其它场合 L=vm_slot_menu(true) 存档菜单、R=vm_slot_menu(false) 读档菜单**。
4. **CG 图片浏览**（ALLPIC.MES 播放中 waiting==1）：A/Y/触屏只在当前组内翻图（var32[9] 恒 1，不链跳到下组）；
   **B → `vm_jump_at("ALLPIC.MES",0x41)` 返回相册菜单**；平时 B=hide。
5. **存读档限制**：`vm_slot_menu` 仅 st.waiting==1 且**非回放脚本**（ALLPIC / EVENT0X / OVER* / CREDITS）
   可调出；标题ロード(util38)不经此函数不受影响。
6. 快进：键盘 X / 手柄 Y（Switch 交换为 X 键）= ff_hold（快进对话；CG 播放中只组内推进不回跳）。

### ⚠️ kind44 LR 问题（用户实测，请你修）
- 用户报告：**相册(44)菜单里 LR 键有 bug**（现象细节请实机复现确认——期望：L=上一页、R=下一页；
  可能的怀疑点：shoulder_page 判定、vm_choose44 的 EXTRA_PREV/NEXT 路径、页边界/重建后 choice_kind 状态）。
- 复现路径：标题 → アルバム（KAWA_TEST_EXTRAS 已解锁 flag200..322，可用
  `KAWA_ENGINESHOT=44 --start ALLPIC.MES` 截图/日志调试）→ 在相册菜单按 L/R。
- 43（场景）翻页与 44 同路径，请一并验证。
- 修复后请保持 41/42/43/44/38/45/46 其余键位行为不变（以上 1-6 为准），并同步两区域源码与 NRO。

> 两区域已同步至：vm.c/frontend.c/kawa.h 一致，NRO MD5 `c2388831`（本镜像 kawaxp.nro 同）。

---

## 🖼 2026-09-08 追加：kind44 相册布局 → 按 PC 原版像素对齐（请你做）

**目标**：kind44(CG 鉴赏)当前可用的布局请对照 PC 原版逐像素校准，做到一致。

**现有实现参照**（extras_ui.c extras_rect/draw kind44 + ALLPIC.MES 驱动）：
- 页：8 页 × 16 格（4×4）；解锁 flag 199..322（album_flag 映射，含 8→321、24→322 特例）
- 格几何（当前）：`(66+132*(i%4), 24+100*(i/4), 112, 84)`，i=页内 0..15
- 底部控制钮（当前，与 AI.exe 内联值一致）：前頁(64,432,80,24)、次頁(228,432,80,24)、
  タイトル(328,432,100,24)、再生(452,432,124,24)；LR 肩键翻页（注意用户报过 LR 有 bug，请一并修）
- 部件：每页 `cg_tm%02u.gcc`(页缩略图素材)、背景 `cg_bg1/2.gcc`、控件条 `cg_pt.gcc`(480×204,
  由 ALLPIC.MES 装载到 surface8)；extras_ui 目前把 cg_tm 的 112×84 区域逐格贴出
- 已逆向确认 util44=0x43ec78（引擎菜单入口），但**格起点/间距/页眉/高亮框等像素参数未与原版核对**

**请你做**：
1. 用原版截图或 AI.exe 数据（0x43ec78 链 / cg_tm01.gcc 自身像素结构）核对 16 格的真实
   x/y 起点与格间距、缩略图贴图区域是否如 112×84、选中框(自 cg_pt 取)位置与样式；
2. 校准页眉（页号显示？）、每页 16 格与底部控制钮的精确坐标后更新 extras_rect(44)/绘制；
3. 修用户报告的 LR 翻页问题，并实机确认整页可点选/翻页与 PC 一致；
4. 完成前后保持两区域源码/NRO 同步，并在 HANDOFF/TO-CODEX 记录结论。

---

## 🩸 2026-09-08：成人 CG "男性器官没显示" —— 实机核验任务（请你做）

**用户反馈**：剧情/回想成人段中男性器官"没显示"（用户推测原版此处应为画面内容/或遮挡效果）。用户无法跑 PC 版，需要实机核验。

**DeepSeek 侧已排查（无系统性问题）**：
1. 脚本层：成人段（如 EVENT02 亜栗栖强X，地址 @5825-@5d20）指令流只有常规 CG 加载 + `Anim("03x.ax")` + 台词，
   **无马赛克/眩光 util 或遮挡指令**；全部 op 已实现（unsupported=0）。
2. 资源层：gcc.ARC/sequence.ARC **无** mosaic/眩光/遮挡类资源；成人画面 = 静态底图（025-037 系列，如
   `036.gcc` 640×480）+ 小动画帧（`036an.gcc` 72×224、`034ban` 76×72、`034dan` 132×132）+ AX 动作。
3. 解码层：036/034b/034d 等底图解码**完整**（无键控残留/无透明空区/无黑补丁）。
4. 渲染实跑：主机跑到成人段截图数值分析 = 正常多彩插画（非黑屏/非缺层）。

**请你实机核验（Switch）**：
- 打开成人回想（シーン2=EVENT02 等）逐段目视：所谓"没显示"是
  (a) 该镜头原画本就无特写（2001 原版构图如此，无需改）？还是
  (b) 某张图/某段 AX 动画确实缺局部（画面异常/空区）？
- 若为 (b)：请记录是哪张 CG（脚本 load_image 的文件名）/哪段 AX，按帧对比底图与渲染输出定位
  （图层、AX 源面/坐标、蒙版合成），修后保持两区域同步。
- 背景信息：EVENT02 成人段静态底图清单 025a/026/027/028a-c/030/034a-d(+an)/035/036/037(+an)，
  AX 动画 = 034a/034b/034d/035/036/037.ax（嘴型/局部，源为各自 an 小图）。
- 复现命令（host）：`KAWA_TEST_EXTRAS=1 KAWA_AUDIO=0 KAWA_MENU43=1 --smoke N --start SCENE.MES` 进回想；EVENT02 需经 SCENE 回想シーン2。
