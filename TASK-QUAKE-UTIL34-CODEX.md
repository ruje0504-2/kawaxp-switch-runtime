> **2026-09-08 本轮特效修复**：util34 改为脚本启停的80ms逐行余弦位移；util35 改为11个软alpha相位+终帧；util8 加入scratch与上下交错条带。host/Switch构建及原始mask像素测试、s10/s12各400消息通过。UI未改。逐帧实机校准仍待完成，证据与限制见 [reports/effects-20260908.md](reports/effects-20260908.md)。旧“435c10=quake/8bit调色板”说明已被反汇编纠正。

# CODEX 交接单：Util34(quake) / Util35(mask 转场) 原版特效精确还原

> **2026-09-08 Codex 核验纠正**：下述“8bit 索引缓冲+调色板”不应作为实现前提。
> `41c100 → 41a040` 每像素读取三个颜色字节、一个独立 alpha 字节；`+0x44` 为 alpha。
> util34 的宽 1920 对应 640×3 个颜色字节。新增证据与未决点见 `reports/title-ui-20260908.md`。
> 当前效果运行实现尚未替换；下文旧结论保留作来源记录，不代表已经验证。

> 来源：DeepSeek 侧（2026-09-08）。目标：按 AI.exe 反汇编实现原版特效；用户无法运行 PC 版，
> 需要你（有实机/可看视频）做最终逐帧核验。当前代码为多版近似，未达原版。

## 结论先行（交给你的核心任务）
原版两个特效跑在 **8bit 索引缓冲 + 调色板** 管线（逐字节读写、页间拷贝、写回调色板级数值），
并涉及**引擎页模型**（[obj+0x80]/[0x84]/[0x88] 三个页面句柄，显示页 ≠ 我们的 surface0）。
建议：仿真 8bpp 双页 + 调色板，把 0x435c10 / 0x43a0b5 两个循环**逐行照搬**，再实机/视频校准。
DeepSeek 侧无视觉、无 Win32 运行环境、页语义与调色板策略未定，RGB 近似已达其上限。

## 涉及代码（当前状态，DeepSeek 与 Codex 树已同步）
- runtime/vm.c `case 34` → `frontend_quake(level)`；`case 35` → `frontend_xfade_start(idx)`
- runtime/frontend.c：`frontend_quake`/`quake_render`（当前=灰纹漂移 v5 占位）、
  `frontend_xfade_start/poll/skip`（mask 渐变 12 步×20ms）、`msk_data[16]` 缓存（%02u.msk 640×480 raw 8bit）
- 更多背景：outputs/KAWAXP-port/HANDOFF.md、work/msk-note.md（DeepSeek+Codex 都有）

## 反汇编证据（AI.exe，2023-10-11 XP 版，ImageBase 0x400000）
### util 派发
- 0x3ee94 起 26 项表 = util#25..50 处理器表：`[9]=0x43e8f5`(util34)、`[10]=0x43eaec`(util35)、
  `[12]=0x43eb2c`(util37 标题)…（表基址=索引+25）
- util34 处理器 0x43e8f5 → 0x40121c → jmp **0x435c10**（quake 核心）
- util35 处理器 0x43eaec → 0x402eb4 → jmp **0x43a0a0**（0x43a0b5 起为函数体，mask 转场核心）
- 处理器参数：util34 推 (0,0,1920,480,0,0)+[obj+0x80]/[0x88]；util35 推 (0,0,640,480,N?,0,0)
  （N=byte[0x50054]=util arg0=msk 索引；util 参数由派发写入对象字段 0x50054/0x50098）

### util34 quake 0x435c10（结构）
- 外层恰 4 相位（[esp+0x2c]=4 递减），相位间 call 0x40134d(→0x40eed0：短等待/可中止)
- 每相位尾：x_off+=2、y_off-=2（mod 8）→ 整帧小对角位移序列 0→±2→±4→±6 级（回跳）
- 内层：8px 块逐字节页间拷贝；源是**宽条带**（1920 宽，左 640=画面+右侧副本）→ 位移不露边
- 读两个 surface 对象（[obj+0x30]=像素基址、[0x34]=行距、[0x12]=宽/字节序、[0xc]=高、[0x44]）

### util35 mask 转场 0x43a0b5（结构）
- sprintf("%02u.msk") 读 640×480×1 遮罩（像素值=揭示时刻 0..255）
- 逐字节：mask 值在 `[th-0x20, th)` 窗口内 → 写回**调色板级数值** `al=(th-mask+phase-0x10)`（夹 0xFF）
  到目标页字节；阈值 th 分 ~12 相位 ×0x20 推进、相位间短等待/可中止
- 结论：8bit 页上按 mask 的「灰度溶解/调色板过渡」，非 RGB 几何替换
- mask 素材：sequence.ARC 00..15.msk（00=瞬时、05/06/07/08/10/12/14 条带、09/11 棋盘、01/02/03 对角、
  15≈软渐变）；脚本经 util35(N,0) 使用，arg0=N=mask 索引（此前误判“无脚本引用”已纠正）

### 页面句柄
- [obj+0x53154]→引擎对象；[+0x80]/[+0x84]/[+0x88] = 页面（显示/暂存/宽条带），精确角色未定；
  [+0x53168] = 某缓冲基址（遮罩/源）。**需你判定显示页 → 我们 surface 的映射**

## 各版本实验与用户反馈（都已在 RGB 上近似，均被否定或存疑）
- v0 整帧 ±3px 平移 → 用户：黑屏处看不见（先因淡出把显示页刷黑）
- v1(util35 误读) 瞬时 copy surfaceN → 糊开机部件(selparts/mwaku)/误黑（已修）
- v2 8×8 瓦片随机错位（含 test@ 灰纹）→ 用户：像水波纹，不对
- v3 行带剪切波 → 用户：仍像水波纹，不对
- v4 四相位整帧对角位移 → 未确认
- v5（当前）灰纹(test@)全屏缓慢漂移 → 用户唯一认可方向：“PC 上是灰底+类似烟雾缓慢移动，原画面
  0%”，但细节（灰度/速度/时机）未核实
- 关键事实：quake 全部 196 处紧跟 kuro 黑场模板（章节起始/冲击切场），显示可能本就黑；
  用户看 PC 流程视频称此类剧情点有「灰底烟雾慢移」→ 疑似就是 mask/quake 把灰纹(test@)显示出来
  缓慢移动。**请实机确认**：这些点的原版画面=灰纹缓慢移动？速度/灰度？是否整屏？

## 剩余未知项（必须实机/视频校准）
1. 显示页与页间拷贝的方向（源/目的页 → 我们哪两个 surface）
2. 调色板策略（CG 自带 8bit 表？固定表？场景自适应？）与 8bpp 量化方式
3. util35 相位时长/可否点击中止；quake 4 相位节奏与帧数
4. quake 触发点是否真为灰纹显示（含 test@ 的 1920 宽条带角色）
5. 我们 surface 0=显示、surface1=暂存 的假定是否成立

## 建议实现路径
1. 先实机/视频定格 quake 点：确认“灰底烟雾慢移”的灰度分布、速度、时长 → 定显示内容=灰纹页?
2. 建 8bpp 页仿真（双页+调色板+宽条带源），照搬两循环 → RGB 上屏
3. 用 PC 视频逐帧 A/B 校准参数
4. 回填本单与 README/HANDOFF

## 附：用户最新原话（要点）
- “我看到别人的pc流程…类似灰色烟雾的缓慢移动，透明度0%（前画面不可见）”——quake 点
- “你没法实现8bit索引缓冲+调色板吗？” → 已说明可行，因缺视频/实机核验而移交
