# PC 存档兼容（PC→Switch 续玩）— 逆向任务单

状态：**已暂停/封存（2026-09-08）**。方向已确认（PC 存档 → Switch 续玩），骨架逆向已完成，
**因无真实游玩存档样本而无法验证字段语义，用户决定暂不实现、备份留档，待日后提供样本再续**。
本文件保留全部逆向结论，避免以后重复劳动。

## 自研引擎存档（已实现，与此任务无关但可对照）
- 本引擎自身剧情存档 = **KWS 格式**（`kawaxp-saves/slot%u.kws`），**早已实现并验证**：
  - 头 4×u32 = `0x3153574b`("KWS1")、版本(1/2)、sizeof(state)、NSURF；gz 压缩；
    v2 另含 AX 播放器状态（文件名/播放位置/延时/循环/图层）
  - 40 槽：F5/LB=存档菜单，F9/RB=读档菜单（对白暂停时），标题菜单ロード（util38）也可进
  - KAWA_AUTOSAVE=N:S 自动存档；smoke 结束做存档→篡改→读档→比对往返测试（SAVE ROUNDTRIP PASS）
  - 读档继续当前画面（NSURF 全部表面）+ AX 动画现场
- 全局旗标兼容：flag0100 = 原版 4336B 格式（5000 个 4-bit @0x540，偶高奇低），已有
- **本次 PC 存档兼容 = 新增"导入原版 PC SaveData/flag 槽"路径**，不影响现有 KWS

## 已确认事实

### 1. PC 原版存档文件集（KAWAXP/savedata/）
| 文件 | 大小 | 含义 |
|---|---|---|
| `FLAG0000-0023`、`FLAG0100` | 4336B | 出厂旗标库（全零）。**FLAG0100 格式已兼容**（0x10F0 负载，flag 区 @0x540，5000×4bit，偶高奇低） |
| `flag0024-0050`（小写） | 4336B | 运行时生成的"进度槽"？头部含 mes 名（open.mes/startup.mes）+ 若干字段，非全零。疑似**剧情进度/续点记录**，与 FLAG0100 布局同尺寸但头部不同 |
| `SaveData0001-0040` | 31144B | **引擎全量剧情存档**（写路径 0x44db10 / 读路径 0x44ff50） |
| `SkipFlag` | 65535B | 跳过已读文本的标记（1 字节/语句？） |

### 2. SaveData%04d 序列化（反汇编证据）
- 写：0x44db10（thunk 0x4016c2 → 由 0x43d45e 等调用，this=系统对象）
  - `lea ebx,[ecx+0x14]`；`rep movs` 0x1e69 dwords = **0x79A4 字节**从源复制到 this+0x14
  - 然后把 this+0x14 起 0x79A4B 写入 `savedata\SaveData%04d`
- 读：0x44ff50（thunk 0x401019）
  - 从文件读 0x79A4B 到 this+0x14（0x4500a7 `lea edi,[esi+0x14]; push 0x79a4`）
- 存档对象位于系统对象 **+0x56100**，数据本体 +0x14 起 0x79A4B（文件 31144B=0x79A8，差 0x14 头部/槽号，含 0x4 版本/槽字段 → 实际文件=0x14 头 + 0x79A4？待核）
- 管理类 0x43d3e0：8 操作 dispatch（槽号 byte arg），涉及 0x40110e/0x401019/0x4016c2/0x4017ee 等

### 3. SaveData0001 空壳内容
```
0000: 02 00 00 00 00 00  "001b.gcc" 00 ...
0x406/0x412-0x41c/0x814: 少量非零（疑似计数器/指针）
```
- 头部 `02` = 版本/档类型；`001b.gcc` = 当前显示 CG 名（标题画面）
- 其余全零 = 空档

### 4. flag0024（小写进度槽）空壳内容
```
0000: "open.mes" 00 "es"...
0x20: u32=320(0x140)   0x28: u32=2000(0x7d0)
0x40: 01 00 00 00
0x68: 0xffff
```
- mes 名 + 320/2000 疑似 var 区尺寸或偏移；0xffff = 空档标记。**注意：这与 FLAG0100 的
  "flag@0x540" 布局不同**——小写 flag%04d 可能是另一结构，或同一文件两种用途。待 RE 0x427b16 区域。

## 重要结论（2026-09-08 复核样本后）
- **本地所有样本（SaveData0001-0040、flag0024-0050、FLAG0xxx）都是出厂/初始化状态，无真实剧情进度**：
  - SaveData 全 40 槽 = 同一空壳（头部 `02 000000 0000 "001b.gcc"`，仅 21 非零字节）
  - flag0024-0050 = 模板内容（`open.mes` + 0x20:u32=320 + 0x28:u32=2000 + 0x40:u32=1 + 0x68:0xffff）
  - FLAG0100 = 全零
- 因此**空壳逆向只能确定骨架（文件名/尺寸/IO 函数），字段语义必须用真实游玩存档回验**。
  即使完整逆向 0x79A4B 布局，没有真实档也无法验证映射正确性。
- 建议下一步：请用户在 PC 原版（或 Wine/VM）里随便推进几行剧情后存档，提供
  `savedata/SaveDataNNNN`（1 个即可）+ 对应小写 `flagNNNN`，即可完成布局核对与续玩实现。

## 逆向进展（2026-09-08 第三轮：记录=资源/场景登记项 ★ 重大）
### 每条 0x40E 记录 = 一个已加载资源/场景状态登记项
- 空壳记录内容（已破译）：
  - rec0: type(u16)=2, 文件名 @+6 = **"001b.gcc"**（标题背景 CG）
  - rec1: type(u16)=0, @+4=0x0e(名长含NUL?), 文件名 @+6 = **"yokan.wav"**（BGM）
  - 尾部 +0x406 有 1-2 字节差异字段（rec0:…01… / rec1:…10…）
- 含义：SaveData = 引擎"当前加载的资源/图层登记表"快照（每条 = CG 页/BGM/SE/图层等）。
  读档时引擎按记录重新加载资源、重建画面 → 所以 PC 档能还原画面现场。
- 结构：文件 = 4B 版本头(u32=2) + N×0x40E 记录；空档仅 2 条活跃（CG+BGM），
  其余全零；0x79A4 = 30 条 × 0x40E 容量。
- 记录内:type @+0 (u16)；名称字段 @+4~+6 起变长；语义按 type 分派。
- **待真实档**：确认 type 全集(0=音频,1=?,2=CG,3=?,…)、每条记录的完整字段
  (句柄/坐标/混合/循环/AX状态)、记录数上限 30 与活跃记录的实际布局。

### SaveData 骨架（前两轮，已定）
- 反汇编证据：
  - 0x44db10（写，thunk 0x4016c2）：`rep movs 0x1e69 dwords`(=0x79A4B) → this+0x14，写文件 0x79A4B
  - 0x44ff50（读，thunk 0x401019）：fread 0x79A4 → this+0x14 → 0x4022d9(→0x44fed0) 应用
  - 0x44fed0（应用）：循环 N 次，每次从数据复制 **0x103 dwords + 1 word = 0x40E 字节**
    记录到栈 → 0x40149c(→0x44fdd0) 逐条恢复；`ebx += 0x40E`
  - 0x44fdd0：复制 0x40E → 0x40149c(→0x44fdd0? 实际=0x44fdd0 自身? 待厘清递归/对象)
- 样本验证：空壳 SaveData0001 非零字节正好按 0x40E 对齐分布在 record0/record1；
  0x79A8 = 4 + 30×0x40E ✓
- 记录内容（空壳线索）：rec0 +0x0=0x02、+0x6..="001b.gcc"；rec1 +0x4..=若干字段。
  0x40E 记录疑似 = 一个"场景页/图层/脚本执行记录"；30 条 = 引擎内部记录上限
- 系统对象字段 0x560f8/0x560fc = 存档数据缓冲区指针/长度（随内容维护）
- 存档对象 = 系统对象 +0x56100，数据本体 +0x14 起；引擎菜单类 0x43d3e0

### flag%04d 槽（0x10F0B）与 SaveData 关系待定
- flag 文件 I/O = 0x427af0/0x427d66（模板 "flag0000"@0x494900），读写 0x10F0B
- 空壳 flag0024-0050 全部同模板（open.mes + 0x20:320 + 0x28:2000 + 0x40:1 + 0x68:0xffff）
  → 可能只是引擎内部固定初始化，非剧情进度；也可能是"快捷续点"（mes 名+地址）
- **待真实档区分**：PC 上"セーブ"写 SaveData%04d 还是 flag%04d（或两者都写）

### 第三轮补充：记录集合架构（已定）
- 0x43d090：取第 N 条记录指针 = `[this+4] + N*0x40E`（记录数组基址在集合对象 +4）
- 0x43d0b0：清空集合；0x43d0f0：集合 resize
- 0x43d1b0（0x40110e 目标）：取第 idx 条记录，越界返回 NULL（槽元数据读取共用）
- 0x44e690（0x401c26 目标）：批量 memcpy N×0x40E（记录快照/复制）
- 0x44fdd0：向记录集合 push 一条 0x40E 记录（容量计算 /0x40E）
- → 引擎运行中持续维护一个"记录集合"（场景/图层/执行状态条目），SaveData 就是它的
  原始快照（30 条上限 × 0x40E）。**记录内部字段语义 = 各记录写入点负责**，需逐点追踪，
  且每条记录可能是不同类型（type byte @+0）**

### 待办
- [ ] 定位运行时"向集合 push 记录"的写入点（0x44de00-0x44e200 密集区），确定记录类型与字段
- [ ] 反汇编 0x44db10 写路径的字段来源（记录从引擎哪些对象序列化）
- [ ] 反汇编 0x43d3e0 dispatch 各 case（槽列表/缩略图、写场景 0x4017ee）
- [ ] 弄清 0x10F0 flag 文件头部与 0x540 区在真实档的填充
- [ ] 产出布局表 + host 解析器 → 接入 load_state
- [ ] **真实存档回验（阻塞）**

## 与现有实现的关系
- 本引擎 KWS 槽格式（slot%u.kws v1/v2）保留不动；新增"导入 PC 档"路径
- FLAG0100 全局旗标兼容已完成（savedata_op / flag_bank）
- 引擎菜单 util37 标题"ロード" util38 当前列 slotN.kws；PC 档导入走新入口或扩 util38
